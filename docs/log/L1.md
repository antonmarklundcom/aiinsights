# L1 — Link pass

## Built
- Verified `main` green first: lint, typecheck, 173 tests, `next build` (CI placeholder
  env), and a clean `drizzle-kit generate` — O1 through S4 are all merged and consistent.
- `README.md`: documented the three features missing from it (screenshot capture, the
  password-gated dashboard/login, the retry cron) and added tag/full-text search to the
  dashboard blurb. No stale `db:push`-as-the-flow or "Instagram or YouTube only" wording
  was found (already cleaned up by O2).
- `KNOWN-ISSUES.md` (new): promoted the still-open cross-phase items out of
  `docs/log/{O1,O2,O3,S1,S3}.md` — duplicate-row migration risk, undercounted cache
  tokens, the non-atomic `attempts` claim, case-sensitive tag matching, missing
  `loading.tsx`, the future-API-route 401-vs-redirect gap, the plain-`Error` from
  `requireSession()`, the Hobby-plan cron cadence, and untested-live screenshot download.
  Resolved items (stale SVGs, the Arial override) were left out.
- Fixed the one small, no-single-owner cross-phase bug found while checking S3's known
  issues against S1's merged code: the item detail page still rendered `item.url` as a
  clickable link unconditionally, so an S3 screenshot capture (`tg://photo/<file_id>`)
  showed as a dead link. `src/app/items/[id]/page.tsx` now renders non-`http(s)` urls as
  plain text (`/* == L1 == */`, one file, 12 lines).
- No `it.todo`/`test.todo` tests exist anywhere in `src/` — nothing to un-skip.
- `plan.md` §9: added the L1 index row.

## Decisions
- The item-URL fix was made directly rather than only logged: it is exactly the "small
  cross-phase fix no single phase could make" case the phase owns (S3 flagged it for S1
  in S3's known issues; S1 merged after S3 but its log shows the fix was never made).
- Left plan §7 (human-inputs checklist) unedited — re-read against the finished build and
  still accurate as written; reused verbatim as the top of the PR body per its own header.
- Did not promote every note from the phase logs to KNOWN-ISSUES.md — internal
  implementation notes (`db-mock.ts`'s condition handling, `after()` not being awaitable,
  wasted `fetchPageMeta` calls on photo-only items) aren't user-facing or cross-phase;
  left in their phase logs.

## Known issues
- None beyond what's now in `KNOWN-ISSUES.md`.

Verification: lint, typecheck, 173 tests, `next build`, and a clean `drizzle-kit generate`
green on this branch after the `items/[id]/page.tsx` fix.
